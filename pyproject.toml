[tool.poetry]
name = "balena-release-tool"
version = "0.1.0"
description = "Release tool to have canary in Balena.io"
authors = ["roger <rogerduran@gmail.com>"]

[tool.poetry.scripts]
balena-release-tool = 'balena_release_tool.cli:cli'

[tool.poetry.dependencies]
python = "^3.5"
click = "^6.7"
balena-sdk = "^7.0"

[tool.poetry.dev-dependencies]
